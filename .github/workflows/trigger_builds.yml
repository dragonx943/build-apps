name: Trigger Doki builds

on:
  workflow_dispatch:

jobs:
  trigger_builds:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo 🧩
        uses: actions/checkout@v4
        
      - name: Trigger builds 🚀
        run: |
          gh workflow run build_release.yml
          gh workflow run build_legacy.yml
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}